[tool.poetry.dependencies]
click = "^8.1.7"
colorama = "^0.4.6"
pyyaml = "^6.0.1"
requests = "^2.31.0"
rich = "^13.7.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
